/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special configuration needed for a simple landing page.
  // You can add image domains or other settings here if needed.
};

export default nextConfig;
